use super::*;

#[test]
fn when_executing_part_one() {
    let input = "";
    let expected = 0;

    assert_eq!(execute_part_one(input), expected);
}

#[test]
fn when_executing_part_two() {
    let input = "";
    let expected = 0;

    assert_eq!(execute_part_one(input), expected);
}